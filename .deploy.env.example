# 部署配置文件
# 复制此文件为 .deploy.env 并填写实际配置信息
# 注意: .deploy.env 已在 .gitignore 中，不会被提交到版本控制

# 服务器配置
SERVER_HOST=your-server-ip-or-domain.com
SERVER_USER=root
DEPLOY_PATH=/opt/stock-scanner

# 端口配置
FRONTEND_PORT=80
BACKEND_PORT=8001

# SSH配置
# 认证方式：使用密码认证时填写 SERVER_PASSWORD，使用密钥认证时填写 SSH_KEY_PATH
SERVER_PASSWORD=your-password-here
# SSH_KEY_PATH=~/.ssh/id_rsa
SSH_PORT=22

# 其他配置
# PYTHON_VERSION=3.9
# NODE_VERSION=18
